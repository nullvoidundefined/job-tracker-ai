pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
    format:
      run: npm run format:check

pre-push:
  commands:
    format:
      run: npm run format
    lint-fix:
      run: npm run lint:fix
    tests:
      run: npm run test:coverage
