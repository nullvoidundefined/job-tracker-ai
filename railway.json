{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "npm run migrate:up && node dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
